services:
  - type: web
    name: muavin-pipeline
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn muavin_api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
